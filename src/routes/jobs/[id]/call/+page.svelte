<script lang="ts">
	let conversationState = $state([]);
	type phraseData = {
		role: string;
		skill: string;
		type: string; // greetings, diploma...
		textPhrase: string;
	};

	// function pushPhrase(phraseData) {
	// 	conversationState.push[phraseData];
	// }
</script>
